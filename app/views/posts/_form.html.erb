<%= form_for(post) do |f| %>
    <p>フォトアップロード場所</p>
    <%= f.text_area :content, size: '70x5', class: 'form-control', placeholder: "内容" %>
    <%= f.submit '投稿', class: 'btn btn-primary' %>
<% end %>
